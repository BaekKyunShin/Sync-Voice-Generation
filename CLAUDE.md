# 프로젝트 지침 (Claude Code 전용)

## 답변 스타일 (가장 중요)

- **항상 한국어**로 답변
- **짧게**: 불필요한 서론·요약·재확인 금지. 간결하게 핵심만 답변.
- **이해하기 쉽게**: 사용자는 음성 데이터를 처음 다뤄봄.
  - 음성·신호처리·anti-spoofing 용어가 나오면 **비유나 일상 표현으로 풀어서** 설명.
  - 영어 약어를 그대로 던지지 말고, 한 줄이라도 풀이를 붙일 것.
- **두괄식**: 결론 먼저, 근거는 뒤에.
- 표·불릿을 적극 활용해 한눈에 보이게.
- 코드 주석은 한글 OK.

## 프로젝트 한 줄 요약

한국어 음성에 대한 **AI 합성 음성 탐지(anti-spoofing)** 모델 만들기 — "사람이 직접 말한 것 vs AI가 합성한 것" 이진 분류. 보이스피싱·딥페이크 방어가 목적. 3주 안에 발표.

상세 기획은 `project_outline.md` 참고.

## 데이터 (확정 사실)

- 진짜(Bona-fide): **KsponSpeech_01 + _02 일부** (한국어 성인 자유발화), 위치 `data_kspon/`
  - _01 화자 17명 / 정제 후 8,787 발화 / 11.80h (확보 완료)
  - _02 일부에서 화자 5~6명 추가 다운 → 합산 학습용 ≈ **15h 목표**
  - **16kHz / 16bit / mono PCM WAV**
  - Speaker-disjoint split 70/15/15 (목표 train 14 / val 4 / test 4명)
- 가짜(Spoofed): **CLOVA 7h + Google 5h + ElevenLabs 3h = 15h** (1:1 균형)
  - 모두 16kHz로 통일 출력
- 텍스트 정제 정책은 `project_outline.md` § 5.4 참고 (망설임 보존, 비언어 마커 제거)

## 반드시 지킬 것 (함정 회피)

1. 진짜·가짜 음성을 **같은 음질·포맷(16kHz/16bit/mono)** 으로 통일 — 음질 차이로 치팅 방지
2. **Speaker-disjoint split**: 같은 사람이 학습용·시험용에 동시에 들어가지 않게
3. **텍스트 누설 금지**: 진짜로 쓴 발화 텍스트는 합성에 절대 사용 X
4. **망설임 보존**: 진짜에만 "어/아/음" 같은 disfluency가 몰리면 모델이 그걸로만 판단 → 정제 룰로 양쪽 균형

## 작업 원칙

- TDD 적용 (예외: 일회성 스크립트, 설정 파일, 데이터 탐색용 노트북)
- 커밋 메시지는 한국어 (`feat:`, `fix:`, `refactor:`, `docs:`, `test:`, `chore:`)
- 작업 완료 전 `verification-before-completion` 호출

## 환경

- 로컬: 맥북 (전처리·시각화)
- 학습/합성: Google Colab Pro/Pro+
- 저장소: Google Drive
